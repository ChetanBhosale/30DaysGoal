"use client";
import { useParams, usePathname } from "next/navigation";
import React from "react";

const View = () => {
  return (
    <div className="w-[50vw] bg-secondary flex justify-center items-center text-4xl font-bold tracking-wider">
      Please Select!
    </div>
  );
};

export default View;
